"use client";

import { Download, LogOut, Moon, ShieldCheck, UserRound } from "lucide-react";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Card, SectionHeading } from "@/components/ui/card";
import { Modal } from "@/components/ui/modal";

function Toggle({ initial = true }: { initial?: boolean }) { const [enabled, setEnabled] = useState(initial); return <button className={enabled ? "switch switch--active" : "switch"} onClick={() => setEnabled(!enabled)} aria-pressed={enabled} aria-label="تبديل الإعداد"><span /></button>; }
export function SettingsView() { const [deleteOpen, setDeleteOpen] = useState(false); return <><div className="page-intro"><div><p className="eyebrow">الحساب</p><h2>حسابي وإعداداتي</h2><p>تفضيلاتك وأهدافك وخصوصية بياناتك.</p></div><Button variant="secondary"><Download size={17} /> تصدير البيانات</Button></div><div className="settings-grid"><Card className="settings-card"><SectionHeading title="الملف الشخصي" /><div className="settings-list"><div className="settings-row"><div><strong>فيصل</strong><small>مستوى متوسط · بناء العضلات</small></div><span className="avatar"><UserRound size={17} /></span></div><div className="settings-row"><div><strong>الطول والوزن</strong><small>176 سم · 78.4 كجم</small></div><Button variant="ghost" size="sm">تعديل</Button></div><div className="settings-row"><div><strong>أيام التدريب</strong><small>3 أيام أسبوعيًا · النادي الرياضي</small></div><Button variant="ghost" size="sm">تعديل</Button></div></div></Card><Card className="settings-card"><SectionHeading title="التفضيلات" /><div className="settings-list"><div className="settings-row"><div><strong>الوضع الداكن</strong><small>اتبع إعداد الجهاز أو بدّله من الأعلى.</small></div><Moon size={18} /></div><div className="settings-row"><div><strong>تذكير الماء</strong><small>كل ساعتين بين 8 ص و10 م.</small></div><Toggle /></div><div className="settings-row"><div><strong>تذكير التمرين</strong><small>قبل الموعد بساعة.</small></div><Toggle initial={false} /></div><div className="settings-row"><div><strong>الوحدات</strong><small>كيلوغرام، سنتيمتر، سعرات حرارية.</small></div><Button variant="ghost" size="sm">تعديل</Button></div></div></Card><Card className="settings-card"><SectionHeading title="الخصوصية والأمان" /><div className="settings-list"><div className="settings-row"><div><strong>حماية البيانات</strong><small>جميع السجلات مملوكة لحسابك فقط.</small></div><ShieldCheck color="var(--lime-deep)" size={22} /></div><div className="settings-row"><div><strong>تغيير كلمة المرور</strong><small>أرسل رابط إعادة التعيين إلى بريدك.</small></div><Button variant="secondary" size="sm">تغيير</Button></div><div className="settings-row"><div><strong>تسجيل الخروج</strong><small>ستنتهي جلستك الحالية على هذا الجهاز.</small></div><Button variant="ghost" size="sm"><LogOut size={15} /> خروج</Button></div></div></Card><Card className="settings-card"><SectionHeading title="منطقة حساسة" /><div className="settings-list"><div className="settings-row"><div><strong>حذف البيانات</strong><small>يتطلب تأكيدًا مزدوجًا ولا يمكن التراجع عنه.</small></div><Button variant="danger" size="sm" onClick={() => setDeleteOpen(true)}>حذف</Button></div></div></Card></div><Modal open={deleteOpen} title="حذف كل البيانات؟" onClose={() => setDeleteOpen(false)}><p>هذا الإجراء يحذف سجلات التمرين والقياسات والصور والتغذية. لن يمكن استعادتها من التطبيق.</p><label style={{ marginTop: "1rem" }}>اكتب «حذف» للتأكيد<input placeholder="حذف" /></label><div className="form-actions"><Button variant="secondary" onClick={() => setDeleteOpen(false)}>إلغاء</Button><Button variant="danger">حذف البيانات</Button></div></Modal></>;
}
